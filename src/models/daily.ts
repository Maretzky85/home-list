export class Daily {
    constructor(id: number, task: string, user_id: number){
        this.id = id,
        this.task = task
        this.user_id = user_id
    }
    id: number
    task: String
    user_id: number
}